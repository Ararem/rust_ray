<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="230" onload="init(evt)" viewBox="0 0 1200 230" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="230" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">Flame Graph</text><text id="details" fill="rgb(0,0,0)" x="10" y="213.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1190" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1190" y="213.00"> </text><svg id="frames" x="10" width="1180" total_samples="319364132000"><g><title> rust_ray::program::check_threads:src\program.rs:225 (135,520,400 samples, 0.04%)</title><rect x="33.3003%" y="101" width="0.0424%" height="15" fill="rgb(227,0,7)" fg:x="106349147700" fg:w="135520400"/><text x="33.5503%" y="111.50"></text></g><g><title> rust_ray::program::&apos;global:src\program.rs:136 (106,516,101,200 samples, 33.35%)</title><rect x="0.0002%" y="133" width="33.3526%" height="15" fill="rgb(217,0,24)" fg:x="534300" fg:w="106516101200"/><text x="0.2502%" y="143.50"> rust_ray::program::&apos;global:src\program.rs:136</text></g><g><title> rust_ray::program::inner:src\program.rs:139 (106,487,663,300 samples, 33.34%)</title><rect x="0.0091%" y="117" width="33.3437%" height="15" fill="rgb(221,193,54)" fg:x="28972200" fg:w="106487663300"/><text x="0.2591%" y="127.50"> rust_ray::program::inner:src\program.rs:139</text></g><g><title> rust_ray::program::process_messages:src\program.rs:143 (31,967,400 samples, 0.01%)</title><rect x="33.3427%" y="101" width="0.0100%" height="15" fill="rgb(248,212,6)" fg:x="106484668100" fg:w="31967400"/><text x="33.5927%" y="111.50"></text></g><g><title>ThreadId(1)-main (106,517,245,400 samples, 33.35%)</title><rect x="0.0000%" y="165" width="33.3529%" height="15" fill="rgb(208,68,35)" fg:x="0" fg:w="106517245400"/><text x="0.2500%" y="175.50">ThreadId(1)-main</text></g><g><title> rust_ray::program::run:src\program.rs:34 (106,516,856,600 samples, 33.35%)</title><rect x="0.0001%" y="149" width="33.3528%" height="15" fill="rgb(232,128,0)" fg:x="388800" fg:w="106516856600"/><text x="0.2501%" y="159.50"> rust_ray::program::run:src\program.rs:34</text></g><g><title> rust_ray::engine::&apos;global:src\engine\mod.rs:44 (106,328,115,000 samples, 33.29%)</title><rect x="33.3532%" y="133" width="33.2937%" height="15" fill="rgb(207,160,47)" fg:x="106518239300" fg:w="106328115000"/><text x="33.6032%" y="143.50"> rust_ray::engine::&apos;global:src\engine\mod.rs:44</text></g><g><title> rust_ray::engine::inner:src\engine\mod.rs:47 (106,318,297,100 samples, 33.29%)</title><rect x="33.3563%" y="117" width="33.2906%" height="15" fill="rgb(228,23,34)" fg:x="106528057200" fg:w="106318297100"/><text x="33.6063%" y="127.50"> rust_ray::engine::inner:src\engine\mod.rs:47</text></g><g><title> rust_ray::engine::process_messages:src\engine\mod.rs:53 (52,861,600 samples, 0.02%)</title><rect x="66.6304%" y="101" width="0.0166%" height="15" fill="rgb(218,30,26)" fg:x="212793492700" fg:w="52861600"/><text x="66.8804%" y="111.50"></text></g><g><title>ThreadId(2)-engine_thread (106,329,232,400 samples, 33.29%)</title><rect x="33.3529%" y="165" width="33.2940%" height="15" fill="rgb(220,122,19)" fg:x="106517245400" fg:w="106329232400"/><text x="33.6029%" y="175.50">ThreadId(2)-engine_thread</text></g><g><title> rust_ray::engine::engine_thread:src\engine\mod.rs:26 (106,328,301,700 samples, 33.29%)</title><rect x="33.3532%" y="149" width="33.2938%" height="15" fill="rgb(250,228,42)" fg:x="106518176100" fg:w="106328301700"/><text x="33.6032%" y="159.50"> rust_ray::engine::engine_thread:src\engine\mod.rs:26</text></g><g><title> rust_ray::ui::event_passthrough:src\ui\mod.rs:253 (621,133,000 samples, 0.19%)</title><rect x="78.5950%" y="101" width="0.1945%" height="15" fill="rgb(240,193,28)" fg:x="251004185400" fg:w="621133000"/><text x="78.8450%" y="111.50"></text></g><g><title> rust_ray::ui::prepare_frame:src\ui\mod.rs:145 (338,606,800 samples, 0.11%)</title><rect x="78.7895%" y="101" width="0.1060%" height="15" fill="rgb(216,20,37)" fg:x="251625318400" fg:w="338606800"/><text x="79.0395%" y="111.50"></text></g><g><title> rust_ray::ui::process_messages:src\ui\mod.rs:644 (13,728,528,300 samples, 4.30%)</title><rect x="78.8955%" y="101" width="4.2987%" height="15" fill="rgb(206,188,39)" fg:x="251963925200" fg:w="13728528300"/><text x="79.1455%" y="111.50"> rust..</text></g><g><title> rust_ray::ui::obtain_data:src\ui\mod.rs:163 (2,474,974,500 samples, 0.77%)</title><rect x="83.7438%" y="85" width="0.7750%" height="15" fill="rgb(217,207,13)" fg:x="267447714300" fg:w="2474974500"/><text x="83.9938%" y="95.50"></text></g><g><title> rust_ray::ui::draw_frame:src\ui\mod.rs:467 (21,310,873,000 samples, 6.67%)</title><rect x="88.8171%" y="69" width="6.6729%" height="15" fill="rgb(231,73,38)" fg:x="283650104900" fg:w="21310873000"/><text x="89.0671%" y="79.50"> rust_ray..</text></g><g><title> rust_ray::ui::maybe_rebuild_font:src\ui\mod.rs:307 (593,287,000 samples, 0.19%)</title><rect x="95.4901%" y="69" width="0.1858%" height="15" fill="rgb(225,20,46)" fg:x="304960977900" fg:w="593287000"/><text x="95.7401%" y="79.50"></text></g><g><title> rust_ray::ui::font_manager::rebuild_font:src\ui\font_manager.rs:261 (59,252,600 samples, 0.02%)</title><rect x="95.6573%" y="53" width="0.0186%" height="15" fill="rgb(210,31,41)" fg:x="305495012300" fg:w="59252600"/><text x="95.9073%" y="63.50"></text></g><g><title> rust_ray::ui::apply_custom_font:src\ui\mod.rs:357 (600,372,300 samples, 0.19%)</title><rect x="97.6589%" y="53" width="0.1880%" height="15" fill="rgb(221,200,47)" fg:x="311887367300" fg:w="600372300"/><text x="97.9089%" y="63.50"></text></g><g><title> rust_ray::ui::main_menu_bar:src\ui\mod.rs:521 (1,032,182,000 samples, 0.32%)</title><rect x="98.3756%" y="37" width="0.3232%" height="15" fill="rgb(226,26,5)" fg:x="314176508700" fg:w="1032182000"/><text x="98.6256%" y="47.50"></text></g><g><title> rust_ray::ui::show_demo_window:src\ui\mod.rs:617 (439,931,300 samples, 0.14%)</title><rect x="98.6988%" y="37" width="0.1378%" height="15" fill="rgb(249,33,26)" fg:x="315208690700" fg:w="439931300"/><text x="98.9488%" y="47.50"></text></g><g><title> rust_ray::ui::redraw:src\ui\mod.rs:158 (50,442,715,600 samples, 15.79%)</title><rect x="83.1942%" y="101" width="15.7947%" height="15" fill="rgb(235,183,28)" fg:x="265692453500" fg:w="50442715600"/><text x="83.4442%" y="111.50"> rust_ray::ui::redraw:sr..</text></g><g><title> rust_ray::ui::outer_render:src\ui\mod.rs:298 (46,212,480,300 samples, 14.47%)</title><rect x="84.5188%" y="85" width="14.4702%" height="15" fill="rgb(221,5,38)" fg:x="269922688800" fg:w="46212480300"/><text x="84.7688%" y="95.50"> rust_ray::ui::outer_r..</text></g><g><title> rust_ray::ui::outer_build_ui:src\ui\mod.rs:355 (10,580,904,200 samples, 3.31%)</title><rect x="95.6758%" y="69" width="3.3131%" height="15" fill="rgb(247,18,42)" fg:x="305554264900" fg:w="10580904200"/><text x="95.9258%" y="79.50"> ru..</text></g><g><title> rust_ray::ui::build_ui:src\ui\mod.rs:512 (3,647,429,500 samples, 1.14%)</title><rect x="97.8468%" y="53" width="1.1421%" height="15" fill="rgb(241,131,45)" fg:x="312487739600" fg:w="3647429500"/><text x="98.0968%" y="63.50"></text></g><g><title> rust_ray::ui::show_metrics_window:src\ui\mod.rs:623 (486,547,100 samples, 0.15%)</title><rect x="98.8366%" y="37" width="0.1523%" height="15" fill="rgb(249,31,29)" fg:x="315648622000" fg:w="486547100"/><text x="99.0866%" y="47.50"></text></g><g><title> rust_ray::ui::event_loop_internal:src\ui\mod.rs:109 (103,822,671,600 samples, 32.51%)</title><rect x="66.6473%" y="133" width="32.5092%" height="15" fill="rgb(225,111,53)" fg:x="212847573000" fg:w="103822671600"/><text x="66.8973%" y="143.50"> rust_ray::ui::event_loop_internal:src\ui\mod.rs:109</text></g><g><title> rust_ray::ui::process_ui_event:src\ui\mod.rs:123 (94,471,201,900 samples, 29.58%)</title><rect x="69.5755%" y="117" width="29.5810%" height="15" fill="rgb(238,160,17)" fg:x="222199042700" fg:w="94471201900"/><text x="69.8255%" y="127.50"> rust_ray::ui::process_ui_event:src\ui\mod.rs:123</text></g><g><title> rust_ray::ui::request_redraw:src\ui\mod.rs:143 (535,075,500 samples, 0.17%)</title><rect x="98.9889%" y="101" width="0.1675%" height="15" fill="rgb(214,148,48)" fg:x="316135169100" fg:w="535075500"/><text x="99.2389%" y="111.50"></text></g><g><title> rust_ray::ui::font_manager::iter_font_dir:src\ui\font_manager.rs:75 (448,307,100 samples, 0.14%)</title><rect x="99.2506%" y="85" width="0.1404%" height="15" fill="rgb(232,36,49)" fg:x="316970816400" fg:w="448307100"/><text x="99.5006%" y="95.50"></text></g><g><title> rust_ray::ui::font_manager::internal_iter:src\ui\font_manager.rs:77 (413,155,100 samples, 0.13%)</title><rect x="99.2616%" y="69" width="0.1294%" height="15" fill="rgb(209,103,24)" fg:x="317005968400" fg:w="413155100"/><text x="99.5116%" y="79.50"></text></g><g><title> rust_ray::ui::font_manager::load_fonts:src\ui\font_manager.rs:149 (1,944,927,100 samples, 0.61%)</title><rect x="99.3910%" y="85" width="0.6090%" height="15" fill="rgb(229,88,8)" fg:x="317419123500" fg:w="1944927100"/><text x="99.6410%" y="95.50"></text></g><g><title> rust_ray::ui::font_manager::process_group:src\ui\font_manager.rs:151 (1,939,982,500 samples, 0.61%)</title><rect x="99.3925%" y="69" width="0.6075%" height="15" fill="rgb(213,181,19)" fg:x="317424068100" fg:w="1939982500"/><text x="99.6425%" y="79.50"></text></g><g><title> rust_ray::ui::init_ui:src\ui\mod.rs:705 (2,693,832,500 samples, 0.84%)</title><rect x="99.1565%" y="133" width="0.8435%" height="15" fill="rgb(254,191,54)" fg:x="316670244600" fg:w="2693832500"/><text x="99.4065%" y="143.50"></text></g><g><title> rust_ray::ui::create_font_manager:src\ui\mod.rs:792 (2,418,459,600 samples, 0.76%)</title><rect x="99.2427%" y="117" width="0.7573%" height="15" fill="rgb(241,83,37)" fg:x="316945617500" fg:w="2418459600"/><text x="99.4927%" y="127.50"></text></g><g><title> rust_ray::ui::font_manager::reload_fonts_list:src\ui\font_manager.rs:49 (2,417,126,200 samples, 0.76%)</title><rect x="99.2431%" y="101" width="0.7569%" height="15" fill="rgb(233,36,39)" fg:x="316946950900" fg:w="2417126200"/><text x="99.4931%" y="111.50"></text></g><g><title>all (319,364,132,000 samples, 100%)</title><rect x="0.0000%" y="181" width="100.0000%" height="15" fill="rgb(226,3,54)" fg:x="0" fg:w="319364132000"/><text x="0.2500%" y="191.50"></text></g><g><title>ThreadId(3)-ui_thread (106,517,654,200 samples, 33.35%)</title><rect x="66.6470%" y="165" width="33.3530%" height="15" fill="rgb(245,192,40)" fg:x="212846477800" fg:w="106517654200"/><text x="66.8970%" y="175.50">ThreadId(3)-ui_thread</text></g><g><title> rust_ray::ui::ui_thread:src\ui\mod.rs:58 (106,516,659,500 samples, 33.35%)</title><rect x="66.6473%" y="149" width="33.3527%" height="15" fill="rgb(238,167,29)" fg:x="212847472500" fg:w="106516659500"/><text x="66.8973%" y="159.50"> rust_ray::ui::ui_thread:src\ui\mod.rs:58</text></g></svg></svg>